flask>=3.0.0
requests>=2.31.0
apscheduler>=3.10.4
meshtastic>=2.3.0
pypubsub>=4.0.3
pyserial>=3.5
cryptography>=42.0.0
# SOCKS5 / HTTP proxy support for the Telegram bridge (and anything else that
# wants `requests.get(..., proxies={"https": "socks5://..."})`).
PySocks>=1.7.1

# MQTT publishing to Home Assistant (weather + mesh telemetry + alerts).
paho-mqtt>=1.6.1

# Keyless DuckDuckGo backend for the /ai web-search tool (web_search.py). Only
# needed when web_search.enabled=true with provider=duckduckgo; the searxng and
# tavily providers use plain requests and need nothing extra.
ddgs>=6.0



# MeshCore mirror — mirror broadcasts into a MeshCore network via a Companion
# node (USB serial / BLE / TCP). Only needed if you enable the MeshCore card.
meshcore>=2.3.0

# Optional — only needed for the "С API" / Telethon mode of the Telegram bridge
# (Прочее → Telegram-мост → режим с api_id). The default "Без API" / web-scrape
# mode works without it. Uncomment if you want MTProto support:
# telethon>=1.34.0
